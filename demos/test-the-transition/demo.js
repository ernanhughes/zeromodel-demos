"use strict";
const cases={pass:{before:{agent:70,enemy:280},after:{agent:130,enemy:280},expected:true},wrong:{before:{agent:130,enemy:280},after:{agent:70,enemy:280},expected:false},collateral:{before:{agent:70,enemy:280},after:{agent:130,enemy:240},expected:false},missing:{before:{agent:70,enemy:280},after:{agent:70,enemy:280},expected:false}};
const select=document.querySelector("#case"),before=document.querySelector("#before"),after=document.querySelector("#after"),diff=document.querySelector("#diff");
function draw(ctxState,canvas,mode="normal"){const ctx=canvas.getContext("2d");ctx.clearRect(0,0,canvas.width,canvas.height);ctx.fillStyle="#0b1120";ctx.fillRect(0,0,canvas.width,canvas.height);ctx.strokeStyle="#26304a";for(let x=0;x<canvas.width;x+=40){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,canvas.height);ctx.stroke()}if(mode==="diff")return;ctx.fillStyle="#60a5fa";ctx.beginPath();ctx.arc(ctxState.agent,110,18,0,Math.PI*2);ctx.fill();ctx.fillStyle="#fb7185";ctx.beginPath();ctx.arc(ctxState.enemy,110,18,0,Math.PI*2);ctx.fill()}
function drawDiff(a,b){const ctx=diff.getContext("2d");ctx.clearRect(0,0,360,220);ctx.fillStyle="#080c16";ctx.fillRect(0,0,360,220);ctx.fillStyle="#fde047";[a.agent,b.agent].forEach(x=>{ctx.beginPath();ctx.arc(x,110,22,0,Math.PI*2);ctx.fill()});if(a.enemy!==b.enemy)[a.enemy,b.enemy].forEach(x=>{ctx.fillStyle="#f472b6";ctx.beginPath();ctx.arc(x,110,22,0,Math.PI*2);ctx.fill()})}
function evaluate(item){const changed=item.before.agent!==item.after.agent;const right=item.after.agent>item.before.agent;const enemyStable=item.before.enemy===item.after.enemy;return[{label:"Agent position changed",ok:changed},{label:"Movement was right / toward enemy",ok:changed&&right},{label:"Enemy position remained stable",ok:enemyStable}]}
function render(){const item=cases[select.value];draw(item.before,before);draw(item.after,after);drawDiff(item.before,item.after);const checks=evaluate(item);document.querySelector("#checks").innerHTML=checks.map(c=>`<div class="check"><span>${c.label}</span><strong class="${c.ok?"pass":"fail"}">${c.ok?"PASS":"FAIL"}</strong></div>`).join("");const ok=checks.every(c=>c.ok);const verdict=document.querySelector("#verdict");verdict.className=`verdict ${ok?"pass":"fail"}`;verdict.textContent=ok?"CONFORMS — visible transition matches contract":"VIOLATION — visible transition does not match contract"}
select.addEventListener("change",render);render();