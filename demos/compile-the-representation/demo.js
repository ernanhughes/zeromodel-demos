"use strict";const requirement=document.querySelector("#requirement"),button=document.querySelector("#compile"),ctx=document.querySelector("#artifact").getContext("2d");const candidates=[{id:"pooled",label:"Pooled summary",cost:1,preserves:["presence"]},{id:"max",label:"Max aggregation",cost:2,preserves:["presence"]},{id:"spatial",label:"Spatial grid",cost:3,preserves:["presence","direction","distance"]},{id:"labelled",label:"Labelled spatial grid",cost:4,preserves:["presence","direction","distance","identity"]}];button.addEventListener("click",compile);requirement.addEventListener("change",compile);function compile(){const need=requirement.value;document.querySelector("#contract").textContent=JSON.stringify({required_evidence:need,must_remain_decodable:true,selection_rule:"lowest-cost passing candidate"},null,2);const tested=candidates.map(c=>({...c,pass:c.preserves.includes(need)}));const list=document.querySelector("#candidates");list.replaceChildren(...tested.map(c=>{const li=document.createElement("li");li.className=c.pass?"pass":"fail";li.textContent=`${c.label} · cost ${c.cost} · ${c.pass?"PASS":"REJECT"}`;return li}));const selected=tested.find(c=>c.pass);draw(selected);document.querySelector("#selection").textContent=`Selected: ${selected.label} (cost ${selected.cost})`;document.querySelector("#trace").textContent=tested.map(c=>`${c.pass?"accept":"reject"} ${c.id}: ${c.pass?`preserves ${need}`:`cannot decode ${need}`}`).join("\n")+`\nselect ${selected.id}: first passing candidate by cost`;}function draw(c){ctx.clearRect(0,0,360,220);ctx.fillStyle="#10253b";ctx.fillRect(0,0,360,220);if(c.id==="spatial"||c.id==="labelled"){ctx.strokeStyle="#315273";for(let x=0;x<=360;x+=45){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,220);ctx.stroke()}for(let y=0;y<=220;y+=44){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(360,y);ctx.stroke()}ctx.fillStyle="#39d98a";ctx.fillRect(90,88,36,36);ctx.fillStyle="#ff6b6b";ctx.fillRect(270,88,36,36);ctx.strokeStyle="#f5d76e";ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(145,106);ctx.lineTo(230,106);ctx.stroke();if(c.id==="labelled"){ctx.fillStyle="#eef4ff";ctx.font="15px system-ui";ctx.fillText("agent:A",78,145);ctx.fillText("target:A",254,145)}}else{ctx.fillStyle="#79d9ff";ctx.fillRect(80,75,200,70);ctx.fillStyle="#07111f";ctx.font="18px system-ui";ctx.fillText(c.label,115,115)}}compile();